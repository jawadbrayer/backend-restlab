# Copy this file to `.env` and edit values as needed.
# Any variable left unset falls back to its default.

# Where the cached JSON files (usersData, mattressData, videoData, optionsData) are stored.
# Default if unset: ~/.restlab
# RESTLAB_DATA_DIR=C:\Users\jawad\Desktop\restlab-data
